language: java
jdk: oraclejdk8
services: docker
env: SPRING_PROFILES_ACTIVE=travis-ci
before_install:
  - nvm install 6
  - docker-compose up -d
script:
  - npm run travis-ci
  - gradle clean build
after_script:
  - docker-compose kill
  - true & cat ./*.log
cache.directories:
  - /var/lib/docker
  - $HOME/.gradle
  - $HOME/.m2
  - node_modules
  - .gradle
  - gradle
notifications.on_failure: change # always|never
